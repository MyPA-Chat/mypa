# Twenty CRM Connection — PA Default Template
#
# Deployed per-PA. Contains the CRM URL and API key.
# Generated during provisioning by the Twenty CRM setup step.
#
# Template variables:
#   {{CRM_URL}}     — Twenty CRM internal URL (e.g., http://172.19.0.4:3000)
#   {{CRM_API_KEY}} — JWT API key (Bearer auth)

TWENTY_CRM_URL={{CRM_URL}}
TWENTY_CRM_API_KEY={{CRM_API_KEY}}
